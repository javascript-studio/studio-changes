{
  "name": "@studio/changes",
  "version": "1.0.0",
  "description": "Generate changes as part of npm version",
  "bin": {
    "changes": "changes.js"
  },
  "scripts": {
    "test": "eslint .",
    "preversion": "npm test",
    "version": "node changes.js",
    "postversion": "git push --follow-tags && npm publish"
  },
  "keywords": [
    "changes",
    "version",
    "release"
  ],
  "author": "Maximilian Antoni <mail@maxantoni.de> (http://maxantoni.de/)",
  "eslintConfig": {
    "extends": "@studio"
  },
  "dependencies": {
    "editor": "^1.0.0"
  },
  "devDependencies": {
    "@studio/eslint-config": "^1.0.0",
    "eslint": "^3.3.1"
  }
}
